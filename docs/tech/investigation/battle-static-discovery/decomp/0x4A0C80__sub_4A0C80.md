# sub_4A0C80 @ 0x4A0C80

- Instr (live): 11
- Palier: high
- GLM A: fence_found=true finish_reason=stop reasoning_tokens=46 (effort_used=high)
- GLM B: fence_found=true finish_reason=stop reasoning_tokens=0 (effort_used=high)
- GLM C: fence_found=true finish_reason=stop reasoning_tokens=46 (effort_used=high)
- A==B: non
- Push IDB: oui
- SetType: int __cdecl sub_4A0C80(int)
- Notes parent: lea*3+lea*5+[eax*4] = arg*0x3C BYTE @ byte_1D2B348. xor ecx puis 8A CL (zero-ext). Deux cdecl, add esp,8 groupé. EAX = sub_4B6E00. Pas de jcc. Occupancy 1+2 / 0xD0 / 0x1D0 / 0x44 absents. Queue morte E3b après MenuSprite_DrawCallback.

## C réconcilié

```c
/* sub_4A0C80 @ 0x4A0C80
 * Ground truth = live ASM (asm_clean.asm) + ida_dump_bytes, not Hex-Rays.
 * 11 instr, size 0x23, end 0x4A0CA3. cdecl, 1 arg, retn C3. No frame.
 * Occupancy 1+2 / slot 0xD0 / F_CHAR 0x1D0 / GF Exists 0x44 absent.
 * Stride: lea [eax+eax*2] *3, lea [eax+eax*4] *5, [eax*4] => arg_0*0x3C.
 * BYTE 8A CL after xor ecx,ecx (zero-ext). No 66. No jcc/setcc/jpt.
 * add esp,8 batches two cdecl 4-byte args (no cleanup after first call).
 * Unreferenced E3b tail after MenuSprite_DrawCallback (nops 0x4A0C7B..0x4A0C7F).
 */

extern char byte_1D2B348[]; /* 0x1D2B348 = dword_1D2B330+0x18, item_size=1 */
extern int __cdecl sub_4B60C0(int);
extern int __cdecl sub_4B6E00(int);

int __cdecl sub_4A0C80(int arg_0)
{
    unsigned int scaled; /* EAX after the two LEAs = arg_0 * 15 */
    unsigned int rec_byte; /* ECX: xor then mov cl */

    scaled = (unsigned int)arg_0;
    scaled = scaled + scaled * 2; /* lea eax,[eax+eax*2] */
    scaled = scaled + scaled * 4; /* lea eax,[eax+eax*4] */
    rec_byte = (unsigned char)byte_1D2B348[scaled * 4]; /* 8A; addr = 0x1D2B348 + arg_0*0x3C */

    /* push ecx; call sub_4B60C0; push eax; call sub_4B6E00; add esp,8 */
    return sub_4B6E00(sub_4B60C0((int)rec_byte));
}
```
