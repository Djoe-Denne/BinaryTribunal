# sub_681270 @ 0x681270

- Instr (live): 33
- Palier: high
- GLM A: fence_found=true finish_reason=stop reasoning_tokens=6
- GLM B: fence_found=true finish_reason=stop reasoning_tokens=0
- GLM C: fence_found=true finish_reason=stop reasoning_tokens=0
- A==B: non
- Push IDB: oui
- SetType: int sub_681270(void)
- Notes parent: clone BdLink pump (IDA: 0x508434). call [node+8] cdecl add esp,4. TEST AL,2 unlink (pas occupancy 1+2). WORD 66 C7 [node+0]=0. TEST EDI puis MOV (ZF préservé). dword_2508128 head / dword_250812C tail. EAX=kept (EBX). 0xD0/0x1D0/GF+0x44 absents. Caller GF_277Carbuncle_SpawnOverlayController.

## C réconcilié

```c
/* sub_681270 @ 0x681270
 * Ground truth = live ASM (asm_clean.asm) + ida_dump_bytes, not Hex-Rays.
 * 33 instr, size 0x4E, end 0x6812BE. IDA type int().
 * No args. Saved EBX ESI EDI. retn C3.
 * Indirect FF 56 08 call [node+8](node); add esp,4. TEST AL,2 unlink.
 * 66 C7 06 00 00 WORD [node+0]=0 on unlink (bytes +0,+1).
 * TEST EDI,EDI then MOV WORD then JZ (MOV does not clobber ZF).
 * dword_2508128 head / dword_250812C tail. EAX=kept (EBX).
 * Occupancy / slot 0xD0 / F_CHAR 0x1D0 / GF Exists 0x44: absent.
 * No domain::.
 */

extern unsigned int dword_2508128;
extern unsigned int dword_250812C;

int sub_681270(void)
{
    unsigned int node;
    unsigned int prev;
    unsigned int kept;

    node = dword_2508128;
    prev = 0;
    kept = 0;
    if (node != 0)
    {
        do
        {
            if (((unsigned char)((int (__cdecl *)(unsigned int))*(unsigned int *)(node + 8))(node)) & 2)
            {
                *(unsigned short *)node = 0;
                if (prev != 0)
                    *(unsigned int *)(prev + 4) = *(unsigned int *)(node + 4);
                else
                    dword_2508128 = *(unsigned int *)(node + 4);
            }
            else
            {
                prev = node;
                kept++;
            }
            node = *(unsigned int *)(node + 4);
        }
        while (node != 0);
    }
    dword_250812C = prev;
    return (int)kept;
}
```
