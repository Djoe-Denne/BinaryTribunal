# sub_50D060 @ 0x50D060

- Instr (live): 117
- Palier: high
- GLM A: fence_found=true finish_reason=stop reasoning_tokens=11600 (retry high/65536; auto/0 → length, C vide, rt=8000)
- GLM B: fence_found=true finish_reason=stop reasoning_tokens=11696
- GLM C: fence_found=true finish_reason=stop reasoning_tokens=15518
- A==B: non
- Push IDB: oui
- SetType: int __cdecl(__int16 *, __int16 *, float *, int)
- Notes parent: spline cubique naturelle (Thomas). Aucun callee. `add esp,10Ch` = épilogue. out[0]/out[n-1]=0 dword. Loop1 `jl` : h[i] puis s[i+1]=dy/dx. loc_50D0EB toujours (s[2]/xs[2] stale si n<3). Forward `n>3`, i=2..n-2. Back-sub `fsubr` puis `jg` signed / A[i]. EAX leftover. Occupancy 1+2 / 0xD0 / 0x1D0 / 0x44 absents. Nom `sub_50D060` conservé.

## C réconcilié

```c
/* sub_50D060 @ 0x50D060
 * Ground truth = live ASM (asm_clean.asm) + ida_dump_bytes, not Hex-Rays.
 * 117 instr, size 0x173, end 0x50D1D3. cdecl. No callees. add esp,10Ch is epilogue.
 * Natural cubic spline 2nd derivatives (Thomas). Catalogue name kept (sub_*).
 * arg0 __int16 *xs (movsx word +2); arg_4 __int16 *ys (pointer, not scalar);
 * arg_8 float *out (dword); arg_C int n. Locals h[32]=var_80, s[32]=var_100
 * (var_FC=s[1], var_F8=s[2]). Engine bound 32, no min(n,32).
 * Signed jle/jl/jg. fdiv st,st(1)=st0/st1. fsubr = mem-st0.
 * loc_50D0EB always runs (stale s[2]/h[0]/xs[2] if n<3).
 * Occupancy 1+2 / slot 0xD0 / F_CHAR 0x1D0 / GF Exists 0x44: absent.
 * EAX leftover = back-sub index (no mov eax before retn).
 */
int __cdecl sub_50D060(__int16 *xs, __int16 *ys, float *out, int n)
{
    float h[32];
    float s[32];
    float w;
    int i;

    out[0] = 0.0f;
    out[n - 1] = 0.0f;

    if (n - 1 > 0) {
        for (i = 0; i < n - 1; i++) {
            h[i] = (float)(xs[i + 1] - xs[i]);
            s[i + 1] = (float)(ys[i + 1] - ys[i]) / h[i];
        }
    }

    out[1] = (s[2] - s[1]) - h[0] * out[0];
    s[1] = (float)(2 * (xs[2] - xs[0]));

    if (n - 2 > 1) {
        for (i = 2; i <= n - 2; i++) {
            w = h[i - 1] / s[i - 1];
            out[i] = (s[i + 1] - s[i]) - w * out[i - 1];
            s[i] = (float)(2 * (xs[i + 1] - xs[i - 1])) - w * h[i - 1];
        }
    }

    i = n - 2;
    out[i] = out[i] - h[i] * out[n - 1];
    if (i > 0) {
        do {
            w = h[i] * out[i + 1];
            i--;
            out[i + 1] = (out[i + 1] - w) / s[i + 1];
        } while (i > 0);
    }

    return i;
}
```
