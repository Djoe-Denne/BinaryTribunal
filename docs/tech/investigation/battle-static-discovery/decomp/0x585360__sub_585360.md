# sub_585360 @ 0x585360

- Instr (live): 38
- Palier: high
- GLM A: fence_found=true finish_reason=stop reasoning_tokens=771
- GLM B: fence_found=true finish_reason=stop reasoning_tokens=594
- GLM C: fence_found=true finish_reason=stop reasoning_tokens=139
- A==B: non
- Push IDB: oui
- SetType: int __cdecl sub_585360(int)
- Notes parent: WORD [arg+0Ch] movsx / 66 store. Angle (s16<<10)/8 +0x400. Sin *100>>12. test cl,1 even→neg. 66 A3 word_1D97712=AX. flags & 0x201 ret 0 sans inc. cmp ax,8 SIGNED setnl → 2 sinon 0. Occupancy 1+2 / 0xD0 / 0x1D0 / 0x44 absents.

## C réconcilié

```c
/* sub_585360 @ 0x585360
 * Ground truth = live ASM (asm_clean.asm) + ida_dump_bytes, not Hex-Rays.
 * 38 instr, size 0x6B, end 0x5853CB. cdecl, 1 arg, ESI saved, retn C3.
 * WORD [arg+0Ch] phase: movsx load, 66 mov cx reload, 66 store ax.
 * cdq; and edx,7; add; sar 3 = signed /8 toward 0. Then +0x400.
 * Fixed_Sin4096_Q12 cdecl 1 arg, add esp,4. Scale lea*5*5 shl2 sar 0Ch = (*100)>>12.
 * test cl,1: even -> neg eax. 66 A3 word_1D97712 = AX only.
 * test flags,201h (bits 0|9); jnz: xor eax,eax ret 0 (phase WORD not incremented).
 * lea eax,[ecx+1]; cmp ax,8 SIGNED setnl; 66 store WORD; ret 2 if >=8 else 0.
 * Occupancy 1+2 / slot 0xD0 / F_CHAR 0x1D0 / GF Exists 0x44 absent.
 * No ja/jg. No jump table. No packed struct.
 */

extern __int16 word_1D97712; /* 0x1D97712 WORD item_size=2 */
extern unsigned int battle_to_update_flags_dword_1D96A9C; /* 0x1D96A9C DWORD */

unsigned int __cdecl Fixed_Sin4096_Q12(int); /* 0x56D130; add esp,4 */

int __cdecl sub_585360(int arg_0)
{
    unsigned char *node;
    __int16 phase;
    int angle;
    int scaled;
    unsigned short next;

    node = (unsigned char *)arg_0; /* 8B 74 24 08 ESI=arg_0 */
    phase = *(__int16 *)(node + 0x0C); /* 0F BF 46 0C movsx */

    angle = ((int)phase << 10) / 8 + 0x400; /* C1 E0 0A; 99; 83 E2 07; 03 C2; C1 F8 03; 05 00 04 00 00 */

    scaled = (int)Fixed_Sin4096_Q12(angle); /* 50; E8; 83 C4 04 */
    scaled = (scaled * 100) >> 12; /* 8D 04 80; 8D 04 80; C1 E0 02; C1 F8 0C */

    if ((phase & 1) == 0) /* F6 C1 01; 75 02 */
        scaled = -scaled; /* F7 D8 */

    word_1D97712 = (__int16)scaled; /* 66 A3 WORD AX */

    if (battle_to_update_flags_dword_1D96A9C & 0x201) /* A1; A9 01 02 00 00 */
        return 0; /* 33 C0; 5E; C3 — no [esi+0Ch] write */

    next = (unsigned short)(phase + 1); /* 8D 41 01 AX = CX+1 */
    *(__int16 *)(node + 0x0C) = (__int16)next; /* 66 89 46 0C */

    /* 66 3D 08 00; 0F 9D C1 setnl; 49; 83 E1 FE; 83 C1 02 */
    if ((__int16)next >= 8)
        return 2;
    return 0;
}
```
