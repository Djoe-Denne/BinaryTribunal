# sub_4F6030 @ 0x4F6030

- Instr (live): 87
- Palier: high
- GLM A: fence_found=true finish_reason=stop reasoning_tokens=2142
- GLM B: fence_found=true finish_reason=stop reasoning_tokens=4060
- GLM C: fence_found=true finish_reason=stop reasoning_tokens=1612
- A==B: non
- Push IDB: oui
- SetType: BOOL __cdecl sub_4F6030(int char_id, int magic_id)
- Notes parent: stride 0x98. Magic 32×{BYTE id,amount}. JunctionHP BYTE +0x5C vs magic_id (zero-ext). CurrentHP WORD 66. Snapshot F_CHAR+0x172 movsx. setl signed. Early FALSE after sub_4C3120. Occupancy/GetRandomInt/0xD0/0x44 absents. Nom catalogue sub_4F6030.

## C réconcilié

```c
/* sub_4F6030 @ 0x4F6030
 * Ground truth = live ASM (asm_clean.asm) + ida_dump_bytes.
 * 87 instr, size 0x10E, end 0x4F613E. IDA type BOOL __cdecl(int, int).
 * CharacterData stride 0x98. Magic 32 x {BYTE id, BYTE amount} at +0x10.
 * CurrentHP WORD +0 (66). JunctionHP BYTE +0x5C.
 * F_CHAR 0x1D0 used only as sub_4BFC90 copy size; snapshot CurrentHP at +0x172.
 * Slot 0xD0 / GF Exists 0x44 / occupancy 1+2 / GetRandomInt: unused.
 * jl signed vs 0x20. setl signed (snapshot HP < saved CurrentHP). No ja/jg/jpt.
 * add esp,4 after sub_4C3120; add esp,0Ch after dest+char_id+char_id.
 * Early FALSE still runs sub_4C3120. No Hex-Rays. No domain::.
 */

extern unsigned char SG_ARRAY_CHARA_DATA[]; /* 0x1CFE0E8, stride 0x98 */

unsigned char *__cdecl sub_4C3120(int char_id);
__int16 __cdecl sub_4BFC90(int char_id, __int16 *dest);
__int16 __cdecl MenuMagic_RebuildPartyDerivedState(int char_id);

BOOL __cdecl sub_4F6030(int char_id, int magic_id)
{
    unsigned char fchar_snap[0x1D0]; /* var_1D0: F_CHAR copy, CurrentHP WORD at +0x172 */
    int base;
    unsigned int saved_hp; /* var_1D8: zero-ext WORD CurrentHP */
    int saved_amount;       /* var_1DC: movsx BYTE amount, or 0 */
    int slot;
    int i;
    int snap_hp; /* var_1D4: movsx WORD snapshot HP */

    base = char_id * 0x98; /* lea eax,[edi+edi*8]; lea ebp,[edi+eax*2]; shl ebp,3 */

    saved_hp = *(unsigned short *)&SG_ARRAY_CHARA_DATA[base]; /* 33 C0 / 66 8B AX */

    sub_4C3120(char_id); /* add esp,4; EAX leftover unused; also on FALSE tail */

    /* xor ecx,ecx; mov cl, JunctionHP; cmp ecx, ebx */
    if ((int)(unsigned char)SG_ARRAY_CHARA_DATA[base + 0x5C] != magic_id)
        return 0;

    saved_amount = 0;
    if (magic_id != 0) { /* cmp ebx, edx=0; jz loc_4F60A4 */
        for (i = 0; i < 0x20; i++) { /* jl signed */
            if (magic_id == (int)(signed char)SG_ARRAY_CHARA_DATA[base + 0x10 + 2 * i]) {
                saved_amount = (signed char)SG_ARRAY_CHARA_DATA[base + 0x11 + 2 * i]; /* loc_4F6130 */
                break;
            }
        }
    }

    slot = 0;
    for (i = 0; i < 0x20; i++) {
        if (magic_id == (int)(signed char)SG_ARRAY_CHARA_DATA[base + 0x10 + 2 * i]) {
            slot = i;
            break;
        }
    }
    /* miss: xor eax,eax → slot remains 0 */

    /* dl == 0 at loc_4F60BC */
    SG_ARRAY_CHARA_DATA[base + 0x10 + 2 * slot] = 0;
    SG_ARRAY_CHARA_DATA[base + 0x11 + 2 * slot] = 0;
    SG_ARRAY_CHARA_DATA[base + 0x5C] = 0;

    sub_4BFC90(char_id, (__int16 *)fchar_snap);

    SG_ARRAY_CHARA_DATA[base + 0x10 + 2 * slot] = (unsigned char)magic_id; /* BL */
    snap_hp = *(__int16 *)&fchar_snap[0x172]; /* 0F BF r/m16 */
    SG_ARRAY_CHARA_DATA[base + 0x11 + 2 * slot] = (unsigned char)saved_amount; /* CL */
    SG_ARRAY_CHARA_DATA[base + 0x5C] = (unsigned char)magic_id; /* BL */
    *(unsigned short *)&SG_ARRAY_CHARA_DATA[base] = (unsigned short)saved_hp; /* 66 89 */

    MenuMagic_RebuildPartyDerivedState(char_id); /* then add esp,0Ch */

    /* xor eax,eax; cmp ecx,esi; setl al */
    return snap_hp < (int)saved_hp;
}
```
