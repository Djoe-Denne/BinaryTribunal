# Rapport vague XN

Copie ce fichier vers `vague-A1.md` … `vague-A8.md`,
`vague-B0.md` … `vague-B3.md`, `vague-APPLY-A1.md` …, ou
`vague-LIVE.md`.
Ne flippe pas `[promotion.G22].satisfied`.
N’invente pas d’offset.

```text
Vague : A1 | A2 | … | B3 | APPLY-An | LIVE
Date :
Agent / outil :
Rail : A-extract | A-apply | B-wiki | C-live
G23 core/ commencé : non
Live lancé : non | oui (LIVE seulement)
satisfied proposé : false
Lignes REGISTER touchées :
```

## Périmètre tenu / dépassé

- Tenu :
- Dépassé (et pourquoi) :

## Preuves

- Autorité (IDB EA / kernel SHA+offset / save SHA+offset / DAT) :
- Formule ou layout (octets, stride, bits) :
- Skip nommé si non fermé (`live-only` / `G23-impl`) :

## Code (A-apply seulement)

- Bits `BattleInitRefused` avant → après :
- Fichiers C++ / fixtures :
- Tests : G21 … / G22 … / `validate_contracts` …
- Pages wiki :

## Wiki (B seulement)

- Pages créées / mises à jour :
- Aucun fichier sous `FinalFantasy_VIII_Reimaginated/core/` :

## Live (LIVE seulement)

- EXE SHA-256 :
- DLL SHA-256 (nouvelle, pas v1/v15/v16) :
- PID :
- Enveloppes (chemin + SHA-256) L22-A / B / C :
- `refused_mask` ordinary :
- restore_hash == preimage_hash :
- native_helper_calls / imported_post_init :
- Safety rouge : aucun | (lequel)

## Pour le chat parent

Une phrase : ce que tu recommandes qu’il tranche
(rester fail-closed / Apply / live / pas G23 / flip après
tous les rapports).
Bloqué sur :
