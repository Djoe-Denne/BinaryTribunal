# Rapport session N

Copie ce fichier vers `session-1.md` / `session-2.md` / `session-3.md`.
Ne flippe pas `[promotion.G22].satisfied`.

```text
Session : 1 | 2 | 3
Date :
Agent / outil :
G23 commencé : non
Live lancé : non | oui (session 3 seulement)
satisfied proposé : false | (session 3 : true seulement en recommandation)
```

## Périmètre tenu / dépassé

- Tenu :
- Dépassé (et pourquoi) :

## Preuves / code

- Bits `BattleInitRefused` avant → après (valeur entière si connue) :
- Fichiers C++ / fixtures :
- Tests : G21 … / G22 … / `validate_contracts` …
- Pages wiki :

## Skips nommés

- (id ou bit) : raison, preuve manquante

## Live (session 3 seulement)

- EXE SHA-256 :
- DLL SHA-256 :
- PID :
- Enveloppes (chemin + SHA-256) L22-A / B / C :
- `refused_mask` ordinary :
- restore_hash == preimage_hash :
- native_helper_calls / imported_post_init :
- Safety rouge : aucun | (lequel)

## Pour le chat parent

Une phrase : ce que tu recommandes qu’il tranche
(promouvoir / rester fail-closed / session bis / pas G23).
Bloqué sur :
